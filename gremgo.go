package gremgo

// Instance is a container for the gremgo interface TODO: Fix this doc
type Instance struct {
}

// // NewInstance creates a WebSocket connection to the Gremlin server and returns a gremgo Instance for interaction. TODO: Fix this too
// func NewInstance(host string) (i *Instance, err error) {
// 	if host == "" {
// 		err = errors.New("No argument provided") // TODO: Detailed error
// 		return
// 	}
// 	d := new(websocket.Dialer)
// 	c, resp, err := d.Dial(h, nil)
// 	if err != nil {
// 		return
// 	}
//
// 	return
// }
